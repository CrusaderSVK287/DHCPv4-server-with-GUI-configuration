#ifndef SERVER_TESTS
#define SERVER_TESTS

#include "greatest.h"

SUITE(linked_list);
SUITE(dhcp_options);
SUITE(utils);
SUITE(pool);
SUITE(allocator);
SUITE(packet_parser_builder);
SUITE(lease);
SUITE(dhcp_message_handlers);

#endif // !SERVER_TESTS
